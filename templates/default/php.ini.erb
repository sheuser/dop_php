; Generated by Chef for node <%= node["fqdn"] %>
; Local modifications will be overwritten.
;
; The php configuration file.
<% if node.recipe?('dop_php::ioncube') %>
zend_extension=<%= node['php']['ioncube']['home'] %>/<%= node['php']['ioncube']['version'] %>.so
<% end %>
<% @php.keys.each do |category| %>
<%= "[#{category}]" %>
<% @php[category].keys.each do |key| %>
<%= "#{key} = #{@php[category][key]}" %>
<% end %>
<% end %>